# Мой первый проект на Python 🚀

Это мой учебный репозиторий, где я осваиваю **Git** и **VS Code**.

## Что я уже умею:
* Настраивать локальный репозиторий (`git init`).
* Связывать папку с GitHub (`git remote add`).
* Сохранять версии кода через `commit`.
* Отправлять изменения в облако через `push`.

## Как запустить код:
1. Установите Python.
2. Запустите файл командой:
   ```bash
   python 1.py
   